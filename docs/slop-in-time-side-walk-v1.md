# Side-profile mascot locomotion

Asset: `games/slop-in-time/skin/gameslop/mascot-side-v1.png`.
Generated with the built-in imagegen tool, using `mascot-pixel-v2.png` as the identity/style reference. The source sheet is saved unchanged. The existing Canvas background extraction removes the generated neutral checker backdrop at load time.

Eight full-body poses: four walk poses above four run poses. All face right; the renderer mirrors them for leftward travel. Torso anchors keep stride changes from shifting the whole character. Run passing poses retain their lift above a common baseline. The new sheet replaces the frontal mascot leg articulation; combat, aerial and enemy art retain their existing renderers.

Final generation prompt:

> Create a production GAME SPRITE SHEET based on the attached GameSlop mascot reference. Reference is identity and pixel-art style only. NEW CAMERA: STRICT 90-DEGREE SIDE PROFILE, all sprites FACE RIGHT. The red rounded game-controller creature has a short upward red antenna, angry cream eye in profile, red boxing-glove hands and red chunky boots, dark charcoal cross/D-pad on its front surface facing RIGHT (thus mostly hidden around right edge). Show its red side/back and one dominant eye at the rightmost edge, NOT the frontal two-eyed oval with a centered cross. NO character faces the viewer. Refined SNES 16-bit shaded pixel art, dark clean pixel outlines, red/coral highlights. Exactly EIGHT full-body poses in a precise 4 columns x 2 rows evenly spaced grid, 1536x1024 total, each cell384x512. Each character about260 pixels tall from antenna tip to sole, centered at x192 relative to its cell, boots baseline y420 relative to cell, identical head/body size throughout, ample empty margin and no overlaps. Top row: four consecutive WALK cycle poses: right foot forward contact, legs passing with left knee lifting, left foot forward contact, legs passing with right knee lifting. Opposite arms swing naturally. Bottom row: four RUN cycle poses: longer split stride right foot forward, tucked passing airborne, longer split stride left foot forward, alternate tucked airborne. Running torso leans forward toward RIGHT. Legs attach naturally to body, side-on feet point RIGHT. Near limbs bright red, far limbs shaded dark red to show depth. Crisp consistent sprite scale, no motion trails, NO shadows, no text, no labels, no gridlines, no backdrop scenery. Transparent background with actual alpha. This sheet must make a readable authentic SIDE VIEW walking/running animation, not a front-view character shuffling.

Verification: 59 engine tests, including sustained held attacks slowing at low stamina without a combat lockout; browser checks cover the loaded sheet, left/right poses, Shift, co-op, touch running, and attacking during regeneration. Pose contact sheets are generated by `tools/cdp-drivers/slop-in-time-stamina-qa.js` in `docs/game-screenshots/`.
